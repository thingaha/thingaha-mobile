name: thingaha
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  google_fonts: ^2.0.0
  carousel_slider: ^2.3.3
  shared_preferences: ^2.0.5
  flutter_riverpod: ^0.14.0+3
  circle_flags: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
   - images/
